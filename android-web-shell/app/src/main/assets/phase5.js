(function(){'use strict';if(window.__CDF_PHASE5__)return;window.__CDF_PHASE5__=true;const Core=window.CiftciPhase5Core;if(!Core)throw new Error('phase5_core_missing');
const getEff=t=>typeof window.eff==='function'?window.eff(t):(typeof window.effective==='function'?window.effective(t):(t.ownership==='partnership'?Math.round(t.amount*(Number(t.share)||0)/100):t.amount));
const H=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const tl=k=>typeof window.money==='function'?window.money(k):new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY'}).format((Number(k)||0)/100);
function ensure(){if(!app.productHoldings||typeof app.productHoldings!=='object')app.productHoldings={};if(!app.marketOverrides||typeof app.marketOverrides!=='object')app.marketOverrides={};}
ensure();
const style=document.createElement('style');style.textContent=`
.p5-sectionTitle{display:flex;justify-content:space-between;align-items:end;gap:10px;margin:20px 2px 9px}.p5-sectionTitle h2{margin:0;font-size:21px}.p5-eyebrow{font-size:11px;font-weight:900;letter-spacing:.08em;color:var(--muted);text-transform:uppercase}.p5-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.p5-stat{background:#fff;border:1px solid var(--line);border-radius:18px;padding:15px;box-shadow:var(--shadow)}.p5-stat span{display:block;color:var(--muted);font-size:12px;font-weight:800}.p5-stat b{display:block;margin-top:5px;font-size:20px}.p5-farmCard{background:linear-gradient(160deg,#fff,#f7fbf8);border:1px solid #dbe8df;border-radius:20px;padding:16px;margin-top:10px}.p5-farmTop{display:flex;justify-content:space-between;gap:10px}.p5-farmName{font-size:17px;font-weight:950}.p5-mini{font-size:12px;color:var(--muted);margin-top:4px}.p5-bars{margin-top:12px}.p5-barRow{margin-top:10px}.p5-barHead{display:flex;justify-content:space-between;font-size:12px;font-weight:850}.p5-barTrack{height:7px;background:#e9efeb;border-radius:999px;margin-top:5px;overflow:hidden}.p5-barFill{height:100%;background:var(--green);border-radius:999px}.p5-tools{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}.p5-tool{border:1px solid var(--line);background:#fff;border-radius:18px;padding:16px;text-align:left;min-height:92px}.p5-toolIcon{font-size:24px;display:block;margin-bottom:8px}.p5-tool strong{display:block;font-size:15px}.p5-tool small{display:block;color:var(--muted);margin-top:3px}.p5-ledgerTools{display:grid;grid-template-columns:1fr 120px;gap:8px;margin-bottom:12px}.p5-ledgerTools input,.p5-ledgerTools select{margin:0}.p5-group{border:1px solid var(--line);border-radius:18px;overflow:hidden;margin-top:11px;background:#fff}.p5-groupHead{display:flex;justify-content:space-between;align-items:center;padding:14px 15px;background:#f7faf8;border-bottom:1px solid var(--line)}.p5-groupTitle{font-weight:950}.p5-groupMeta{font-size:12px;color:var(--muted);margin-top:2px}.p5-record{display:grid;grid-template-columns:1fr auto;gap:10px;padding:13px 15px;border-bottom:1px solid var(--line)}.p5-record:last-child{border-bottom:0}.p5-edit{border:1px solid #b8d2c2;color:var(--green);background:#f5faf7;border-radius:11px;padding:8px 10px;font-weight:900}.p5-marketHero{background:linear-gradient(145deg,#123d2d,#1f6b4d);color:#fff;border-radius:22px;padding:18px;margin-bottom:12px}.p5-marketHero .big{font-size:28px;margin:3px 0}.p5-marketRow{display:grid;grid-template-columns:1.05fr .95fr;gap:10px;padding:13px 0;border-bottom:1px solid var(--line)}.p5-price{font-weight:950;text-align:right}.p5-source{font-size:11px;color:var(--muted);line-height:1.35;margin-top:3px}.p5-holdingRow{display:grid;grid-template-columns:1fr auto;gap:10px;padding:12px 0;border-bottom:1px solid var(--line)}.p5-total{font-size:25px;font-weight:950;color:var(--green)}.p5-calcShell{background:#101612;border-radius:26px;padding:17px;color:#fff;box-shadow:0 16px 45px rgba(0,0,0,.16)}.p5-calcDisplay{height:96px;display:flex;align-items:end;justify-content:end;padding:10px 6px;font-size:42px;font-weight:500;overflow:hidden;word-break:break-all}.p5-keypad{display:grid;grid-template-columns:repeat(4,1fr);gap:9px}.p5-key{aspect-ratio:1;border:0;border-radius:999px;background:#29302c;color:#fff;font-size:21px;font-weight:800}.p5-key.op{background:#2f7a58}.p5-key.util{background:#68716b}.p5-key.zero{grid-column:span 2;aspect-ratio:auto;border-radius:999px;text-align:left;padding-left:26px}.p5-quickCalc{margin-top:14px;border:1px solid var(--line);border-radius:18px;padding:15px;background:#fff}.p5-note{padding:11px 12px;background:#f6f8f6;border-radius:12px;color:var(--muted);font-size:12px;line-height:1.45}.p5-editBanner{background:#fff7e6;border:1px solid #ecd8aa;color:#76500e;padding:11px;border-radius:12px;margin-bottom:12px;font-weight:800}.p5-cancelEdit{width:100%;min-height:50px;margin-top:8px;border:1px solid var(--line);background:#fff;border-radius:14px;font-weight:850}@media(max-width:420px){.p5-grid{grid-template-columns:1fr 1fr}.p5-ledgerTools{grid-template-columns:1fr}.p5-calcDisplay{font-size:36px}}
`;document.head.appendChild(style);

function allCrops(){const out=[];Object.values(crops||{}).forEach(list=>list.forEach(c=>{if(!out.includes(c))out.push(c)}));return out;}
function seasonTransactions(){const year=String(new Date().getFullYear());return (app.transactions||[]).filter(t=>String(t.date||'').startsWith(year));}
function renderHomeEnhancements(){const home=document.getElementById('home');if(!home)return;let box=document.getElementById('p5Home');if(!box){box=document.createElement('div');box.id='p5Home';const actions=home.querySelector('.actions');actions?.insertAdjacentElement('afterend',box);}const txs=seasonTransactions();const inc=txs.filter(t=>t.type==='income').reduce((s,t)=>s+getEff(t),0);const out=txs.filter(t=>t.type==='expense').reduce((s,t)=>s+getEff(t),0);const parcels=Core.parcelSummaries(txs,getEff);const top=Core.topExpenseCategories(txs,getEff,3);const max=top.length?top[0].total:1;let holdings=0;ensure();for(const [crop,qty] of Object.entries(app.productHoldings)){const val=Core.productValue(qty,Core.marketFor(crop),app.marketOverrides[crop]);if(val)holdings+=val.mid*100;}
box.innerHTML=`<div class="p5-sectionTitle"><div><div class="p5-eyebrow">${new Date().getFullYear()} sezonu</div><h2>Çiftliğin özeti</h2></div></div><div class="p5-grid"><div class="p5-stat"><span>Sezon gideri</span><b class="out">${tl(out)}</b></div><div class="p5-stat"><span>Sezon sonucu</span><b class="${inc-out>=0?'in':'out'}">${tl(inc-out)}</b></div><div class="p5-stat"><span>Kayıtlı tarla</span><b>${parcels.length}</b></div><div class="p5-stat"><span>Ürün tahmini değer</span><b>${tl(Math.round(holdings))}</b></div></div>${parcels.length?`<div class="p5-sectionTitle"><div><div class="p5-eyebrow">Tarlalar</div><h2>Tarlalarım</h2></div></div>${parcels.slice(0,4).map(p=>`<div class="p5-farmCard"><div class="p5-farmTop"><div><div class="p5-farmName">🌱 ${H(p.parcel)}</div><div class="p5-mini">${H(p.crop)} · ${p.count} kayıt · Son: ${H(p.lastCategory||'—')}</div></div><b class="${p.result>=0?'in':'out'}">${tl(p.result)}</b></div><div class="p5-grid" style="margin-top:12px"><div><div class="p5-mini">Gelir</div><b>${tl(p.income)}</b></div><div><div class="p5-mini">Masraf</div><b>${tl(p.expense)}</b></div></div></div>`).join('')}:''}${top.length?`<div class="p5-sectionTitle"><div><div class="p5-eyebrow">Masraf dağılımı</div><h2>En çok nereye gidiyor?</h2></div></div><div class="card p5-bars">${top.map(x=>`<div class="p5-barRow"><div class="p5-barHead"><span>${H(x.category)}</span><b>${tl(x.total)}</b></div><div class="p5-barTrack"><div class="p5-barFill" style="width:${Math.max(6,Math.round(x.total/max*100))}%"></div></div></div>`).join('')}</div>`:''}`;
}

let ledgerQuery='',ledgerCrop='all';function installLedger(){const rows=document.getElementById('ledgerRows');if(!rows)return;const old=document.getElementById('p3LedgerSegment');if(old)old.style.display='none';let tools=document.getElementById('p5LedgerTools');if(!tools){tools=document.createElement('div');tools.id='p5LedgerTools';tools.className='p5-ledgerTools';tools.innerHTML=`<input id="p5LedgerSearch" placeholder="Kayıtlarda ara…"><select id="p5LedgerCrop"><option value="all">Tüm ürünler</option>${allCrops().map(c=>`<option>${H(c)}</option>`).join('')}</select>`;rows.insertAdjacentElement('beforebegin',tools);document.getElementById('p5LedgerSearch').addEventListener('input',e=>{ledgerQuery=e.target.value.toLocaleLowerCase('tr');renderLedger();});document.getElementById('p5LedgerCrop').addEventListener('change',e=>{ledgerCrop=e.target.value;renderLedger();});}renderLedger();}
function filteredTx(){return (app.transactions||[]).filter(t=>{if(ledgerCrop!=='all'&&t.crop!==ledgerCrop)return false;if(!ledgerQuery)return true;return [t.category,t.crop,t.parcel,t.note,t.date].join(' ').toLocaleLowerCase('tr').includes(ledgerQuery);});}
function recordRow(t){return `<div class="p5-record"><div><div class="title">${H(t.category)}</div><div class="sub">${H([t.crop,t.parcel,fmtDate(t.date),t.note].filter(Boolean).join(' · '))}</div><div class="amount ${t.type==='income'?'in':'out'}" style="margin-top:5px">${t.type==='income'?'+':'−'} ${tl(getEff(t))}</div></div><div><button class="p5-edit" type="button" onclick="phase5EditTx('${t.id}')">Düzenle</button></div></div>`;}
function renderLedger(){const rows=document.getElementById('ledgerRows');if(!rows)return;const txs=filteredTx();if(!txs.length){rows.innerHTML='<div class="empty">Bu filtrede kayıt yok.</div>';return;}const expenses=Core.groupExpenseSections(txs,getEff);const incomes=txs.filter(t=>t.type==='income');rows.innerHTML=expenses.map(g=>`<section class="p5-group"><div class="p5-groupHead"><div><div class="p5-groupTitle">${H(g.category)}</div><div class="p5-groupMeta">${g.items.length} kayıt</div></div><b class="out">${tl(g.total)}</b></div>${g.items.map(recordRow).join('')}</section>`).join('')+(incomes.length?`<section class="p5-group"><div class="p5-groupHead"><div><div class="p5-groupTitle">Gelirler</div><div class="p5-groupMeta">${incomes.length} kayıt</div></div><b class="in">${tl(incomes.reduce((s,t)=>s+getEff(t),0))}</b></div>${incomes.map(recordRow).join('')}</section>`:'');}

let editingId=null;function resetEditMode(){editingId=null;const btn=document.querySelector('#tx .primary');if(btn){btn.textContent='Kaydet';btn.setAttribute('onclick','saveTx()');}document.getElementById('p5EditBanner')?.remove();document.getElementById('p5CancelEdit')?.remove();}
window.phase5EditTx=function(txId){const t=(app.transactions||[]).find(x=>x.id===txId);if(!t)return;editingId=txId;showPage('tx');setTxType(t.type);document.getElementById('parcel').value=t.parcel||'';document.getElementById('cropGroup').value=t.cropGroup||'Genel';fillCropItems();document.getElementById('cropItem').value=t.crop||document.getElementById('cropItem').value;document.getElementById('ownership').value=t.ownership||'self';togglePartner();if(t.partnerId)document.getElementById('partnerSelect').value=t.partnerId;document.getElementById('txCategory').value=t.category||document.getElementById('txCategory').value;renderDetails();Object.entries(t.details||{}).forEach(([k,val])=>{const el=document.getElementById(k);if(el)el.value=val;});document.getElementById('txAmount').value=(t.amount/100).toFixed(2).replace('.',',');document.getElementById('txDate').value=t.date||today();document.getElementById('txStatus').value=t.status||'paid';document.getElementById('txNote').value=t.note||'';const card=document.querySelector('#tx .card');if(card&&!document.getElementById('p5EditBanner')){const b=document.createElement('div');b.id='p5EditBanner';b.className='p5-editBanner';b.textContent='Bu kaydı düzenliyorsun. Kayıt kimliği ve geçmişi korunur.';card.insertBefore(b,card.children[1]||null);}const btn=document.querySelector('#tx .primary');btn.textContent='Değişiklikleri kaydet';btn.setAttribute('onclick','phase5CommitEdit()');if(!document.getElementById('p5CancelEdit')){const cancel=document.createElement('button');cancel.id='p5CancelEdit';cancel.type='button';cancel.className='p5-cancelEdit';cancel.textContent='Vazgeç';cancel.onclick=()=>{resetEditMode();showPage('ledger');};btn.insertAdjacentElement('afterend',cancel);}window.scrollTo(0,0);};
window.phase5CommitEdit=async function(){const t=(app.transactions||[]).find(x=>x.id===editingId);if(!t){toast('Kayıt bulunamadı');return;}let amount;try{amount=parseMoney(v('txAmount'));}catch(e){toast(e.message);return;}let partnerId=null,share=100;if(v('ownership')==='partnership'){partnerId=v('partnerSelect');const p=app.partnerships.find(x=>x.id===partnerId);if(!p){toast('Ortaklık seç');return;}share=p.share;}const d={};document.querySelectorAll('#txDetails input,#txDetails select').forEach(e=>{if(e.value!=='')d[e.id]=e.value;});Object.assign(t,{type:typeof txType!=='undefined'?txType:t.type,amount,parcel:v('parcel').trim(),cropGroup:v('cropGroup'),crop:v('cropItem'),ownership:v('ownership'),partnerId,share,category:v('txCategory'),details:d,status:v('txStatus'),date:v('txDate')||today(),note:v('txNote').trim(),updatedAt:Date.now()});await save();resetEditMode();render();renderLedger();showPage('ledger');toast('Kayıt güncellendi');};
const originalOpenTx=window.openTx;if(typeof originalOpenTx==='function')window.openTx=function(t){resetEditMode();return originalOpenTx(t);};

function installMoreTools(){const grid=document.querySelector('#more .menuGrid');if(!grid)return;[...grid.querySelectorAll('.menuBtn')].forEach(b=>{if(/^Banka/.test(b.textContent.trim()))b.remove();});if(!document.getElementById('p5MarketBtn')){const m=document.createElement('button');m.id='p5MarketBtn';m.className='menuBtn';m.innerHTML='📈 Piyasa & Ürün Değeri<span>Referans fiyatlar ve elindeki ürün</span>';m.onclick=openMarket;grid.prepend(m);}if(!document.getElementById('p5CalcBtn')){const c=document.createElement('button');c.id='p5CalcBtn';c.className='menuBtn';c.innerHTML='🧮 Hesap Makinesi<span>Telefon tipi + tarla hesapları</span>';c.onclick=openCalculator;grid.prepend(c);}}
const wrap=document.querySelector('.wrap');const marketPage=document.createElement('section');marketPage.id='market';marketPage.className='page';marketPage.innerHTML='<div id="p5MarketBody"></div>';wrap.appendChild(marketPage);const calcPage=document.createElement('section');calcPage.id='calculator';calcPage.className='page';calcPage.innerHTML='<div id="p5CalcBody"></div>';wrap.appendChild(calcPage);
function priceText(entry){if(!entry)return 'Referans yok';if(Number.isFinite(entry.ref))return `${entry.ref.toLocaleString('tr-TR',{maximumFractionDigits:2})} TL/${entry.unit}`;return `${entry.low.toLocaleString('tr-TR',{maximumFractionDigits:3})}–${entry.high.toLocaleString('tr-TR',{maximumFractionDigits:3})} TL/${entry.unit}`;}
function holdingValueText(crop,qty){const val=Core.productValue(qty,Core.marketFor(crop),app.marketOverrides[crop]);if(!val)return 'Fiyat gir';const low=val.low,high=val.high;if(Math.abs(low-high)<0.001)return new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY',maximumFractionDigits:0}).format(low);return `${new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY',maximumFractionDigits:0}).format(low)} – ${new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY',maximumFractionDigits:0}).format(high)}`;}
function openMarket(){showPage('market');renderMarket();}
function renderMarket(){ensure();const body=document.getElementById('p5MarketBody');let total=0;for(const [crop,qty] of Object.entries(app.productHoldings)){const val=Core.productValue(qty,Core.marketFor(crop),app.marketOverrides[crop]);if(val)total+=val.mid;}const holdings=Object.entries(app.productHoldings).filter(([,q])=>Number(q)>0);body.innerHTML=`<div class="p5-marketHero"><div class="p5-eyebrow" style="color:#cfe7d8">ELİNDEKİ ÜRÜNLER</div><div class="big">${new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY',maximumFractionDigits:0}).format(total)}</div><div style="opacity:.8;font-size:12px">Referans veya kendi fiyatına göre tahmini değer. Gerçek satış fiyatı değildir.</div></div><div class="card"><h2>Ürün miktarı ekle</h2><label>Ürün</label><select id="p5HoldCrop">${allCrops().map(c=>`<option>${H(c)}</option>`).join('')}</select><div class="grid2"><div><label>Miktar (kg)</label><input id="p5HoldQty" inputmode="decimal" placeholder="Örn. 3500"></div><div><label>Kendi tahmini fiyatın (TL/kg)</label><input id="p5HoldPrice" inputmode="decimal" placeholder="Boş bırakılabilir"></div></div><button class="primary" onclick="phase5SaveHolding()">Ürün değerine ekle</button></div><div class="card"><h2>Ürün değer tablosu</h2>${holdings.length?holdings.map(([crop,qty])=>`<div class="p5-holdingRow"><div><div class="title">${H(crop)}</div><div class="sub">${Number(qty).toLocaleString('tr-TR')} kg · ${app.marketOverrides[crop]?`Kendi fiyatın: ${Number(app.marketOverrides[crop]).toLocaleString('tr-TR')} TL/kg`:priceText(Core.marketFor(crop))}</div></div><div class="p5-price">${holdingValueText(crop,qty)}<br><button class="small" style="margin-top:6px" onclick="phase5EditHolding('${H(crop)}')">Düzenle</button></div></div>`).join(''):'<div class="empty">Elindeki ürün miktarını ekleyince değeri burada görünür.</div>'}</div><div class="card"><h2>Türkiye referans fiyatları</h2><div class="p5-note">Fiyatlar bölge, kalite, çeşit ve teslim şekline göre ciddi değişebilir. Borsa aralığı/üretici ortalaması kaynak ve tarih bilgisiyle gösterilir; satış garantisi değildir.</div>${Core.MARKET.map(e=>`<div class="p5-marketRow"><div><div class="title">${H(e.crop)}</div><div class="p5-source">${H(e.kind)} · ${H(e.source)} · ${H(e.date.split('-').reverse().join('.'))}</div></div><div class="p5-price">${H(priceText(e))}</div></div>`).join('')}</div>`;}
window.phase5SaveHolding=async function(){ensure();const crop=v('p5HoldCrop');const qty=Number(v('p5HoldQty').replace(',','.'));const priceRaw=v('p5HoldPrice').trim().replace(',','.');const price=priceRaw?Number(priceRaw):null;if(!Number.isFinite(qty)||qty<0){toast('Geçerli miktar gir');return;}if(priceRaw&&(!Number.isFinite(price)||price<=0)){toast('Fiyatı kontrol et');return;}app.productHoldings[crop]=qty;if(price)app.marketOverrides[crop]=price;else delete app.marketOverrides[crop];await save();renderMarket();renderHomeEnhancements();toast('Ürün değeri güncellendi');};
window.phase5EditHolding=function(crop){document.getElementById('p5HoldCrop').value=crop;document.getElementById('p5HoldQty').value=String(app.productHoldings[crop]??'').replace('.',',');document.getElementById('p5HoldPrice').value=app.marketOverrides[crop]?String(app.marketOverrides[crop]).replace('.',','):'';window.scrollTo(0,0);};

let calcState=Core.calcInitial();function openCalculator(){showPage('calculator');renderCalculator();}
function renderCalculator(){const body=document.getElementById('p5CalcBody');body.innerHTML=`<div class="card" style="background:transparent;border:0;box-shadow:none;padding:0"><div class="p5-calcShell"><div id="p5CalcDisplay" class="p5-calcDisplay">${H(calcState.display)}</div><div class="p5-keypad">${['C','±','%','÷','7','8','9','×','4','5','6','−','1','2','3','+','0',',','⌫','='].map((k,i)=>`<button class="p5-key ${['C','±','%','⌫'].includes(k)?'util':''} ${['÷','×','−','+','='].includes(k)?'op':''} ${k==='0'?'':''}" onclick="phase5CalcPress('${k}')">${k}</button>`).join('')}</div></div></div><div class="p5-quickCalc"><h2>Tarla kısa hesapları</h2><div class="grid2"><div><label>Dönüm</label><input id="p5Area" inputmode="decimal" placeholder="18"></div><div><label>TL / dönüm</label><input id="p5PerDa" inputmode="decimal" placeholder="1250"></div></div><div id="p5AreaResult" class="calc">Toplam: ₺0,00</div><div class="grid2"><div><label>Ürün (kg)</label><input id="p5Kg" inputmode="decimal" placeholder="3500"></div><div><label>TL / kg</label><input id="p5KgPrice" inputmode="decimal" placeholder="14,75"></div></div><div id="p5SaleResult" class="calc">Satış: ₺0,00</div></div>`;['p5Area','p5PerDa'].forEach(id=>document.getElementById(id).addEventListener('input',()=>{const a=parseLocal(v('p5Area')),b=parseLocal(v('p5PerDa'));document.getElementById('p5AreaResult').textContent='Toplam: '+new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY'}).format(a*b||0);}));['p5Kg','p5KgPrice'].forEach(id=>document.getElementById(id).addEventListener('input',()=>{const a=parseLocal(v('p5Kg')),b=parseLocal(v('p5KgPrice'));document.getElementById('p5SaleResult').textContent='Satış: '+new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY'}).format(a*b||0);}));}
function parseLocal(x){const n=Number(String(x||'').trim().replace(/\./g,'').replace(',','.'));return Number.isFinite(n)?n:0;}
window.phase5CalcPress=function(key){calcState=Core.calculatorPress(calcState,key);const d=document.getElementById('p5CalcDisplay');if(d)d.textContent=calcState.display;};

const oldRender=window.render;if(typeof oldRender==='function')window.render=function(){oldRender();renderHomeEnhancements();installLedger();installMoreTools();};const oldRenderAll=window.renderAll;if(typeof oldRenderAll==='function')window.renderAll=function(){oldRenderAll();renderHomeEnhancements();installLedger();installMoreTools();};
installMoreTools();renderHomeEnhancements();installLedger();
})();